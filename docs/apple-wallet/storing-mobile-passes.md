---
title: Storing mobile passes
weight: 26
---

# Storing mobile passes

`save()` stores the compiled payload in `MobilePass`. Apple device registrations are stored in `AppleMobilePassDevice` and `AppleMobilePassRegistration`. The PassKit routes use those tables to answer update checks and notify devices.
